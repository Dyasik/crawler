/**
 * This is an example config file.
 * Copy its content to a new file named `conf.js` and customize as needed.
 */

module.exports = {
    get EmailLogin() {
        return ''
    },
    get EmailPass() {
        return ''
    },
    get EmailTo() {
        return '' // CSV
    },
    get TimeoutMin() {
        return NaN
    },
    get TimeoutMax() {
        return NaN
    },
}
